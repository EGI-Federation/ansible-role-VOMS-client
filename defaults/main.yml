---
# defaults file for voms-client
prerequisites:
  redhat:
    '6':
      - voms-clients3
      - fetch-crl
    '7':
      - voms-clients-cpp
      - fetch-crl
  ubuntu:
    16:
    - voms-clients
    - fetch-crl

voms_dir:  /etc/grid-security/vomsdir
vomses_dir: /etc/vomses
vo_source_of_truth: http://cclavoisier01.in2p3.fr:8080/lavoisier/voms_vo?accept=json